{
  "description": "Parallel quality gates for Claude Code completion",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/wrapper.sh gates",
            "timeout": 70000
          }
        ]
      }
    ]
  }
}
